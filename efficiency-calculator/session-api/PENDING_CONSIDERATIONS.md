# 미반영 고려사항 — record-actions-code 분자 (넣을까 말까 결정용)

작성 2026-08-31 (§66~§68 감사 직후). 기준 표본: 이 PC 최근 55세션(200KB 초과),
분모 13,812분. 현재값(§70 후) **ON/ON 75,165분 5.42배 · OFF/OFF 109,366분 7.89배**.
기대효과 %는 그 조합의 현재 분자 대비. 재측정은 `bench_4combos.py`(고정 목록, 같은 폴더) 방식 —
§7.1 표와 같은 절차로 4조합 전후 + 역전(ON>OFF) 세션 수를 함께 낼 것.

전제 두 개 (변경 금지):
- 분자는 ON·OFF 모두 **해당 업무 숙련자** 기준. OFF = 숙련자가 AI 기록을 그대로
  재연, ON = 자기 방식. "숙련자도 했을 일인가"로만 판정한다.
- **항목별 ON ≤ OFF**. OFF만 올리는 항목은 안전, ON만 올리는 항목은 OFF가
  상한, 공통 가산은 순서 불변.

## 1. 우선 검토 (근거 명확, 크기 있음)

| # | 항목 | 모드 | 기대효과 | 위치 | 근거 / 주의 |
|---|---|---|---|---|---|
| 1 | **Write 중간판 폐기** — 같은 파일을 여러 번 Write하면 마지막 판만 계상 | OFF | **+3,602분 (+3.3%)** | `requirement_actions.py` 총량 `writes[-1]` | OFF 정의("기록 그대로")와 어긋남(60,036단어, 118파일). 단 사람은 다시 쓸 때 전체를 다시 치지 않음 → "덧붙여 다시 쓰기"는 최종본만이 맞고 **"갈아엎기"(앞판과 겹침 없음)만 확실한 과소**. 넣으려면 앞판과 겹치지 않는 단어만 가산(diff) 권고. ON 순계는 그대로 → 순서 안전 |
| 2 | **실행 출력 판독 200단어 규칙** — 호출당 앞 200단어 정독, 나머지 훑기 | OFF | ≈ **+1,700분 (+1.6%)** (훑기 450wpm 반영 후) | `exec_item`, `requirement_actions.py` `ex_deep/ex_skim` | rw OFF는 "읽기 전량 정독"인데 실행 출력만 훑기 → 같은 모드 안 불일치. OFF 분기에서 `out_words × rr`. ON은 그대로 → 순서 안전 |
| 6 | **도구 중간 생각 미계상** | 공통 | **+1,802분 (+2.5%)** | `record_actions_code_api.py` `measure` think 블록 | §59 검토 후 미채택(숙련자는 도구 사용 중 생각 안 씀). 그러나 `rates.json` `think._comment`·`human_exec_model._comment_mech`는 "생각 전량 계상으로 옮겨 기계조작 0.25로 낮춤"이라 서술 → **주석과 코드 불일치**. 계상하거나 주석을 고쳐야 함. 계상 시 `mid_tokens × 0.75 × 0.005` |

## 2. 판단 사항 (근거 약하거나 정책)

| # | 항목 | 모드 | 기대효과 | 위치 | 근거 / 주의 |
|---|---|---|---|---|---|
| 7 | **메인 마무리 답변(파일 산출물 있을 때) 0** | 공통 | **+322분 (+0.4%)** | `build_actions` 보고형 폴백 | 파일이 있으면 답변 미계상. 인수인계 메모·커밋 메시지 수준은 씀. 소량 |
| 8 | **메인 진행 나레이션 167,851단어** | 공통 | think 요율이면 **+839분 (+1.1%)** | — | §68("서브 보고 = 밖으로 나온 생각") 논리를 연장하면 일관. 반론: 사용자용 발화라 생각과 다름 |
| 12 | **Grep/Glob 결과 판독 0** — `_SEARCH_TOOLS`는 읽기 단위가 안 됨(전체 도구 결과의 30%) | ON(rw) | **+394분 → 450wpm이면 ≈ +1,500분** | `requirement_actions.py` `_SEARCH_TOOLS` `continue` | "검색 2분/건이 결과 훑기 포함"으로 해석하면 현행. 계상하면 실행 출력과 같은 눈금(앞 200 정독·나머지 훑기). OFF는 `reviewed`에 이미 포함 → 가산 금지 |

## 3. 되돌리기/버그 (값을 낮추는 방향)

| # | 항목 | 모드 | 기대효과 | 위치 | 근거 |
|---|---|---|---|---|---|
| 9 | **서브 전략 생각 하한 제거** — 서브 517지점 전부 토큰 미기록 → §58 하한 1.5분 균일 가산 | 공통 | **−776분 (−1.1%)** | `_scan_strategy_thinking` `_is_sub`일 때 `fallback` 미집계 | §68에서 들어감. 서브당 상수라 세션 간 변별력 없음(서브 많이 띄운 세션만 커짐). 서브 프롬프트는 부모가 이미 계획한 일이라 서브의 첫 생각은 전략보다 실행 준비에 가까움. 토큰 기록된 서브 생각은 유지 |
| 10 | **`replay_write_net` 폴백 "덧붙임"** — 생성 파일 재생 중 `old_string`을 못 찾으면 new를 이어붙임 | ON | ≈ **−150분** | `replay_write_net` 생성 파일 분기 | 45파일 2,535단어. 순계 > 총량 가능. `min(순계, 총량)` 상한 |

## 4. 요율 없음

| # | 항목 | 크기 |
|---|---|---|
| 11 | 이미지 118블록(스크린샷 판독) | 미계상, 요율 없음 |
| — | 미등록 쓰기 도구 2,494단어 | §38 경보 대상 |

## 합계 (9 제외)

- OFF만: 1+2 ≈ +5,300분 (+4.9%) — 순서 안전
- ON만: 12 ≈ +1,500분 (+2%) — OFF 상한 안
- 공통: 6+7+8 ≈ +2,960분 (ON +4.0% / OFF +2.7%)
- 전부 넣으면 대략 ON +7%, OFF +7%. 9·10 넣으면 각 −1%.

반영 완료(목록에서 제거): #3 실패 명령 판정 좁힘(§69, +1,250분) · #4 조회형 전량 정독(§70, 실측 +105분 — 사전 추정 +800은 경로 분류 오류) · #5 검색 턴당 하한(§70, +424분).

## 이번 감사에서 **넣지 않기로 확정**한 것 (재검토 불필요)

- "코드 이해 시간을 고친 코드량에 비례해 추가" — 분자는 숙련자 기준(문서 §3)이라 평균 개발자 문헌(Xia 58%)을 목표로 삼는 것은 자의 목적 오인. 철회.
- "ON > OFF 허용" — 불변식 위반. 철회.
- `subagent_type`(Plan/Explore)별 요율 차등 — 통로 이름으로 면제/차등하는 데서 구멍이 났던 §59 교훈.
- 문서 78% 정독 0 · 검색 40회→1건 · 헛읽기 0 · 반복 명령 작성 0 · 썼다 지운 글 0 — 숙련자는 안 하는 일. 설계 의도.
